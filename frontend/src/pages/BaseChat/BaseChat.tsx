import BaseTemplate from "../../templates/BaseTemplate/BaseTemplate";

export const route = "/base_chat";

const BaseChatPage = () => {
  return (
    <BaseTemplate>
      <div>
        <h1>Base Chat</h1>
      </div>
    </BaseTemplate>
  );
};

export default BaseChatPage;
